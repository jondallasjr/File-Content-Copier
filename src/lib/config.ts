export const CONFIG = {
    STATUS_TIMEOUT: 3000,
    MAX_FILE_SIZE: 5 * 1024 * 1024, // 5MB
    KEYBOARD_SHORTCUTS: {
      SELECT_ALL: ['Meta+a', 'Control+a'],
      COPY: ['Meta+c', 'Control+c'],
    }
  } as const;